# Development Baseline

This isolated development copy starts from the submitted `0.5.0+codex.20260828040118` archive through the isolated r7 candidate, not from an installed cache.

Scope for this copy:

- Bind every follow-up refresh to the original selected workspace.
- Keep one visible business material package per run while refresh snapshots remain under the hidden control directory.
- Persist a concise terminal follow-up result and return it through one foreground completion wait, without Codex heartbeats.

This copy is not in a Marketplace and is not a submitted or frozen artifact.
