# Recruiting Evidence Agents r18 发布审查

审查日期：2026-08-25

审查依据：`grill-me-sessions/d13-release-readiness.grill.md` 的已决业务约束，以及本发布目录的代码和离线测试。

## 本轮已关闭

- 新增 `$hrd-ceo-review-agent`，将 HRD 意见确认、CEO 包构建、预览和明确投递组织为对话流程。业务用户不需要提供脚本命令、token 或本地路径。
- CEO 包以隔离交接目录定义完整 cohort，不再只围绕“已有 HRD 意见”的候选人构建。HRD 面试通过者缺少完整意见时阻断；零名 HRD 面试通过者仍可生成事实性包。
- `ceo_summary_message.md` 成为第六项正式产物。消息以完整通过、明确未通过或退出、状态未能确认三类互斥计数为主，异常单列为覆盖层；不会把 HRD 建议数伪装成流程结果。
- 投递预览和发送均绑定已生成的 CEO 消息原文；传入不同正文会停止。外部投递仍只包含消息、审阅 HTML 与纯材料 ZIP。
- 删除已废弃的 `People Ops审核状态` schema 依赖；当前 Base 字段 `name/id` 形态与单选值数组均由适配层归一化。
- 当存在已保存 HRD 意见时，CEO 包必须读取同批次 `hrd_opinions_saved` 审计事件；缺少经 `SAVE` 确认的审计记录会阻断构建。
- 支持显式 `--table-id` 选择多表 Base 中的目标数据表，并以精确 `--batch` 创建同表的独立批次交接；未指定批次的多批次表会阻断，快照复用也绑定同一表和批次范围。
- Base block 目录兼容当前 `blocks` 返回形态；指定表不属于当前 Base、不可见或不是数据表时会在读取附件前阻断。
- 增加只读 `--list-tables` 与 `--list-batches` 发现模式：业务人员先选择数据表；单批次仅提示当前材料范围，多批次展示所有批次及记录数后再选择。
- 批次发现后始终要求业务人员确认；即使只有一个批次，材料构建也必须显式传入 `--batch`，不会自动下载附件。
- HRD 与 CEO 子流程接受已确认的 `--table-id`，可继续使用原始 Base 根链接，不要求业务人员手工拼接 URL query。
- Base intake 将材料发现分为活动门禁与非阻断追溯：`Invalid Material`、`Missing Material`、`Unmapped Material` 仍阻断；`Misplaced Material`、`Duplicate Submission`、`Multiple Material Versions` 会写入正式内部清单，并在审阅页只显示聚合追溯提示，不催办、不自动选择“更完整”的版本。
- 无效附件现在按材料组件的可用副本判定：同一 Base 字段已有可用副本时，报告 `Invalid Extra Material` 并建议只删除无效附件；只有没有可用副本时才是需要补交的活动 `Invalid Material`。
- 收到“生成催办预览”时，`处理状态` 会作为内部流程标记同步写入并读回，不再向业务用户展示或索取拟写入确认。
- 收到一次“确认发送并监听10分钟”时，单一受控入口完成全部消息预检、发送、回执登记和有界监听；全部已发送事项回复后立即刷新 Base，未收齐则持续至 10 分钟结束。
- 监听改为受控的独立本地进程，避免 Codex 前台命令超时回收监听；本地控制审计记录启动、每轮轮询、结束或失败，已有发送回执可只恢复监听而不重发消息。
- 每个独立催办控制目录持有自己的飞书发送幂等键；重试仍可安全复用本轮发送，但新测试或新批次不会复用历史消息回执。
- 业务对话与错误文案不再使用内部阶段名；`处理状态` 仅由催办预览自动同步，HRD 意见流程不再将其列为待确认写入项。
- P2P 普通消息未返回线程关联字段时，监听器对同一收件人的合格回复按已发送顺序一对一匹配，并在审计中标记关联方式；线程关联仍优先。
- 监听启动成功后，当前 Codex 任务创建一分钟一次的受控心跳；后台监听完成或失败时，心跳自动回到原任务读取审计与 Base 刷新结果，再停止自身。
- 监听回传改为单次守候心跳，等待期间不输出中间进展；材料问题关闭后直接进入 HRD 意见队列并使用逐行意见格式。

## 验证证据

- `cd plugins/recruiting-evidence-agents && python3 -m unittest discover -s tests -p 'test_*.py' -v`：99 项通过。
- 新增覆盖：当前 CLI 的 `name/id` 字段形态、无 `People Ops审核状态` schema、单选流程状态归一化、HRD `SAVE` 审计事件与 CEO 构建绑定、显式多表选择、不混批交接、表/批次发现结果、根链接与已选表 ID 传递、当前 Base block 返回形态，以及非阻断追溯发现的交接读取与 HTML 呈现。
- 补充覆盖：仅字节与 SHA-256 完全相同的材料才是重复提交，重复提交不会生成催办或删除要求；错放材料会按实际内容归入对应候选人材料目录，不生成催办、清理或确认事项；无效额外材料和不同版本可生成仅供业务人员确认的核查与清理草稿，不发送消息、不自动删除或选择保留版本。
- 补充覆盖：一次确认可发送全部预览项、登记实际回执并立即启动监听；监听器在全部已发送事项收到关联回复时提前结束并统一刷新。
- 补充覆盖：监听进程以独立会话派生，发送后的超时不会结束监听；只恢复已有回执的监听不会再次调用消息发送。
- 补充覆盖：不同控制目录生成不同发送幂等键，同一控制目录可稳定复用该键。
- 补充覆盖：两条不带线程字段的合格 P2P 回复按发送顺序分别匹配同一收件人的两项催办；已用回复不能再次关联。
- 补充覆盖：发送和恢复监听结果明确要求任务心跳，仅在监听实际启动时创建。
- 补充覆盖：单次守候心跳禁止等待中的中间输出，HRD 意见提示只保留逐行格式。

## 未关闭项

- 仍需以重新安装后的 ZIP 运行一次不发送的端到端验收；离线测试不能替代安装包验收。
- 30/180 天本地留存清理机制和已投递候选人的流程更新/更正交付尚未实现，不能声明为完整发布就绪。
- 子集处理必须先创建独立交接目录；当前版本不从全量材料 ZIP 重打包子集，以避免遗漏或静默重用。

## 当前结论

可进入合成数据的干跑与人工安装验收；在完成上述端到端授权验收和未关闭项之前，不声明生产发布就绪或 Evidence Freeze。
